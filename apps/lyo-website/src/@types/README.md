# @types Folder Convention

See `apps/lyo-dashboard/src/@types/README.md` for detailed explanation of why we use `@types` folders.

## Quick Summary

- **`@types/`** = Type definitions only (compile-time)
- Separates types from runtime code
- Aligns with TypeScript ecosystem conventions
- Better IDE support and type resolution
