{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@lyo/lyo-extension-mvp",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt",
        "cwd": "{projectRoot}"
      }
    },
    "dev:chrome": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt -b chrome",
        "cwd": "{projectRoot}"
      }
    },
    "dev:firefox": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt -b firefox",
        "cwd": "{projectRoot}"
      }
    },
    "dev:edge": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt -b edge",
        "cwd": "{projectRoot}"
      }
    },
    "dev:safari": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt -b safari",
        "cwd": "{projectRoot}"
      }
    },
    "prepare": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt prepare",
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt build",
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["prepare"],
      "outputs": ["{projectRoot}/dist"]
    },
    "build:chrome": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt build -b chrome",
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["prepare"],
      "outputs": ["{projectRoot}/dist"]
    },
    "build:firefox": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt build -b firefox",
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["prepare"],
      "outputs": ["{projectRoot}/dist"]
    },
    "build:edge": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt build -b edge",
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["prepare"],
      "outputs": ["{projectRoot}/dist"]
    },
    "build:safari": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt build -b safari",
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["prepare"],
      "outputs": ["{projectRoot}/dist"]
    },
    "zip": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt zip",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "zip:chrome": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt zip -b chrome",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "zip:firefox": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt zip -b firefox",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "zip:edge": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt zip -b edge",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "zip:safari": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wxt zip -b safari",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "compile": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "{projectRoot}"
      }
    }
  }
}
