export enum AuthProvider {
  GOOGLE = 'google',
  LOCAL = 'local',
}

export enum JobStatus {
  STARTING = 'starting',
  IN_QUEUE = 'in_queue',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  FAILED = 'failed',
}
